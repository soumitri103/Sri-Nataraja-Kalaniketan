# NPM Configuration for Security
# This file configures npm to handle dependency security vulnerabilities

# Audit settings
audit-level=moderate

# Force overrides defined in package.json
# This prevents vulnerable transitive dependencies from being installed
